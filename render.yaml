services:
  # ---------- Frontend ----------
  - type: web
    name: recruitai-frontend
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    pullRequestPreviews: true
    routes:
      - type: rewrite
        source: /api/*
        destination: https://recruitai-backend.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_BACKEND_URL
        value: https://recruitai-backend.onrender.com
      - key: VITE_AI_URL
        value: https://recruitai-ai.onrender.com

  # ---------- Backend ----------
  - type: web
    name: recruitai-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    healthCheckPath: /actuator/health
    envVars:
      - key: DATABASE_URL
        fromService: recruitai-db
        fromProperty: connectionString
      - key: DATABASE_USERNAME
        fromService: recruitai-db
        fromProperty: username
      - key: DATABASE_PASSWORD
        fromService: recruitai-db
        fromProperty: password
      - key: REDIS_URL
        fromService: recruitai-redis
        fromProperty: connectionString
      - key: AI_SERVICE_URL
        value: https://recruitai-ai.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        value: "*"

  # ---------- AI Service ----------
  - type: web
    name: recruitai-ai
    env: docker
    dockerfilePath: ai-service/Dockerfile
    dockerContext: ai-service
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromService: recruitai-db
        fromProperty: connectionString
      - key: DATABASE_USERNAME
        fromService: recruitai-db
        fromProperty: username
      - key: DATABASE_PASSWORD
        fromService: recruitai-db
        fromProperty: password
      - key: REDIS_URL
        fromService: recruitai-redis
        fromProperty: connectionString

  # ---------- Managed MySQL ----------
  - type: postgres
    name: recruitai-db
    plan: starter

  # ---------- Managed Redis ----------
  - type: redis
    name: recruitai-redis
    plan: starter
